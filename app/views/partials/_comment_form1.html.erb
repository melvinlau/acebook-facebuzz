<%= simple_form_for @comment do |comment| %>

  <%= comment.input :body, as: :text%>
  <br><br>
  <%= comment.button :submit, 'Add Comment' %>

<% end %>

<%= form_with(url: '/comments', method: 'post') do %>
  <%= text_field_tag(:message) %>
  <%= submit_tag('Add Comment') %>
<% end %>

<%= simple_form_for @comment do |comment| %>
  <%= comment.input :message, as: :text%>
  <br><br>
  <%= comment.button :submit, 'Add Comment' %>
<% end %>
<%= form_with(url: '/comments', method: 'post') do %>
  <%= text_field_tag(:message) %>
  <%= submit_tag('Add Comment') %>
<% end %>

<%= simple_form_for @comment do |comment| %>
  <%= comment.input :message, as: :text%>
  <br><br>
  <%= comment.button :submit, 'Add Comment' %>
<% end %>